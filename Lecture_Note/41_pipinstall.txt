1. pypi 로 구글에 검색
2. 첫번째 pypi.org 사이트로 접속
3. 원하는 페키지를 터미널에서 pip install (페키지명)을 입력해서 설치
4. pip list를 통해 현재 설치된 페키지의 내용을 알 수 있음
5. pip show (페키지명)을 입력해 페키지 정보를 알 수 있음
6. pip install --upgrade (페키지명)을 통해 페키지의 최신 버전을 업그레이드 할 수 있음
7. pip uninstall (페키지명)을 통해 설치된 페키지를 제거할 수 있음